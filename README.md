# Пример автотеста на Behave

Перед установкой тестов необходимо убедится что установлен chromedriver

## Установка
```bash
python3.6 -m virtualenv venv
source venv/bin/activate
pip install -r requirements.txt
```
## Запуск
```bash
python -m behave
```
